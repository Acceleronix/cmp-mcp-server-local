# CMP MCP Server Configuration
# Copy this file to .env and fill in your actual values

# Required: CMP API Credentials
CMP_API_KEY=your_api_key_here
CMP_API_SECRET=your_api_secret_here

# Optional: API Configuration
CMP_API_ENDPOINT=https://cmp.acceleronix.io/gateway/openapi

# Note: Page size and format are now smart defaults (no configuration needed)
# - SIM/eUICC queries default to 10 items per page (optimized for token efficiency)
# - Statistical queries default to 100 items for better analysis
# - All responses use compact format by default
# Users can override these per-request when calling the tools
